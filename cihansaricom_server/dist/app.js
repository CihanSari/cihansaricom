"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const bodyParser = require("body-parser");
const express = require("express");
const expressWs = require("express-ws");
const cookieParser = require("cookie-parser");
const logger = require("morgan");
const subdomain = require("express-subdomain");
const index_1 = require("./routes/index");
const api_1 = require("./routes/api");
const lavalandroute_1 = require("./routes/lavalandroute");
const clothing_1 = require("./routes/clothing");
const app = express();
exports.app = app;
expressWs(app);
app.use(logger("dev"));
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: false }));
app.use(cookieParser());
app.use(subdomain("lavaland", lavalandroute_1.getRouter()));
app.use(subdomain("clothingweb", clothing_1.getRouter()));
app.use("/", index_1.getRouter());
app.use("/api", api_1.getRouter());
